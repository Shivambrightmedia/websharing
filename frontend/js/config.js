/* =========================================
   CONFIGURATION
   Update SERVER_URL with your deployed backend URL
   ========================================= */

const CONFIG = {
    // Change this to your deployed backend URL (e.g., Render, Railway)
    // For local testing: 'http://localhost:3000'
    SERVER_URL: 'http://localhost:3000',

    // Cloudinary settings
    CLOUDINARY: {
        CLOUD_NAME: 'derb7wswl',
        UPLOAD_PRESET: 'web123'
    }
};
